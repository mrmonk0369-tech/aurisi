if(sm)sm.textContent=(STU.files.length>1?(STU.files.length+" files · "):"")+(STU.doc?"document · ":"")+(code.length/1024).toFixed(1)+" KB · self-check "+(ferr?"review":"OK")+(fixes?(" · "+fixes+" fix"):"")+" · run "+(rerr?"review":"OK")+(rt?(" · "+rt+" fix"):"");