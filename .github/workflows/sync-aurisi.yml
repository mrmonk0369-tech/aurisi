name: Build AURISI v59.1
on: [push, workflow_dispatch]
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          set -e
          test -f AURISI.html
          if grep -q 'APP_VERSION="59.1"' AURISI.html; then echo "already v59.1 - nothing to do"; exit 0; fi
          test "$(wc -c < AURISI.html)" -eq 1177328
          python3 .github/scripts/build_v591.py AURISI.html AURISI_new.html
          test "$(wc -c < AURISI_new.html)" -eq 1177374
          mv AURISI_new.html AURISI.html
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add AURISI.html
          git commit -m "chore: build AURISI v59.1 (Master Access Control)" || echo "no changes"
          git push
